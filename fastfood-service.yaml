apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: app-service
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: app
  type: ClusterIP