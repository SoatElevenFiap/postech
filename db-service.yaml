apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: db-service
  namespace: fastfood
spec:
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30432
  selector:
    app: db
  type: NodePort